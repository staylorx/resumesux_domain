name: domain_contracts
resolution: workspace
description: Domain contracts and abstractions for Resume Suckage clean architecture
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  domain_entities:
    path: ../domain_entities
  fpdart: ^1.2.0

dev_dependencies:
  lints: ^6.0.0