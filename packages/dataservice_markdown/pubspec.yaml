name: dataservice_markdown
resolution: workspace
description: Markdown dataservice for Resume Suckage clean architecture
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  domain_contracts:
    path: ../domain_contracts
  datasource_markdown:
    path: ../datasource_markdown

dev_dependencies:
  lints: ^6.0.0
  test: ^1.28.0